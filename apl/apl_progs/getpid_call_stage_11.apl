integer main()
{	
	integer PID,a,parentPID;	
	//PID=Getpid();
	//print(PID);
	//PID=Fork();
	//PID=Fork();
	// parentPID=Getppid();
	// print(parentPID);
	// a=Wait(1);
	// print(a);
	a=Signal();
	print(a);
	// a=Signal();
	// print(a);
	return 0;
}